<header>
    {{#if user}}
        <div id="name-display">
            <i data-lucide="user" style="color:white"></i> 
            <button type="button" id="logout">Log out</button>
        </div>
    {{/if}}
</header>